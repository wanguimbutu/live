# Set this to your production ERPNext URL to proxy API calls there during dev.
# Leave empty to use the local bench instance (default).
# Example: VITE_FRAPPE_URL=https://mycompany.erpnext.com
VITE_FRAPPE_URL=
